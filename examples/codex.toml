# Add to ~/.codex/config.toml

[mcp_servers.google-ads]
command = "npx"
args = ["-y", "google-ads-mcp-unofficial@0.1.0"]
# All secrets stay in ~/.google-ads-mcp/config.json after `setup`; nothing to pin here.
